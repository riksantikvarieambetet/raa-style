{
  "name": "raa-style",
  "version": "2.1.0",
  "description": "Sass och CSS-bibliotek för Riksantikvarieämbetet. Demo fins på http://styleguide.raa.se/.",
  "main": "",
  "scripts": {
    "start": "npm run build && node-sass -w src --output dist/css",
    "clean": "rm -Rf dist",
    "build": "npm run build:css && npm run build:copy-scss && npm run build:list-icons && npm run build:copy-assets",
    "build:css": "node-sass src --output dist/css",
    "build:copy-scss": "mkdirp dist/scss && cp src/* dist/scss",
    "build:list-icons": "node list-icons.js",
    "build:copy-assets": "cp -R assets dist/",
    "prepublish": "npm run clean && npm run build"
  },
  "repository": "http://stash.raa.se/projects/GRAP/repos/raa-style/browse",
  "keywords": [
    "sass",
    "css",
    "raa",
    "style"
  ],
  "author": "Riksantikvarieämbetet",
  "license": "MIT",
  "dependencies": {
    "normalize-scss": "^7.0.0"
  },
  "devDependencies": {
    "mkdirp": "^0.5.1",
    "node-sass": "^4.7.2"
  },
  "private": false
}